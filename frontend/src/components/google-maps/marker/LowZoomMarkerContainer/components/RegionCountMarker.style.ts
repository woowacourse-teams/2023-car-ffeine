import styled from 'styled-components';

export const StyledRegionCountMarker = styled.div`
  background: #fff;
  border-radius: 8px;
  border: 1px solid #2a6cd8;
  padding: 5px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 5px;
`;

export const StyledRegionName = styled.div`
  font-size: 1.2rem;
  font-weight: bold;
`;

export const StyledRegionCount = styled.div`
  background: #d0e1fa;
  border-radius: 4px;
  border: 1px solid #2a6cd8;
  font-size: 1.2rem;
  font-weight: bold;
  padding: 0px 2px;
`;
